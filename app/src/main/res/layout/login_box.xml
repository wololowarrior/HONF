<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout android:layout_width="fill_parent"
    android:layout_height="match_parent"

    xmlns:android="http://schemas.android.com/apk/res/android">
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"

    android:layout_height="wrap_content"
    android:background="#fff"
    android:layout_width="match_parent"
    android:paddingTop="10dp">
    <EditText android:layout_height="wrap_content"
        android:id="@+id/txtUsername"
        android:layout_width="match_parent"
        android:hint="Username">
        <requestFocus></requestFocus>
    </EditText>
    <EditText android:layout_height="wrap_content"
        android:inputType="textPassword"
        android:id="@+id/txtPassword"
        android:layout_width="match_parent"
        android:hint="*****">
    </EditText>
    <LinearLayout android:layout_width="match_parent"
        android:layout_marginTop="10dp"
        android:layout_height="wrap_content" android:id="@+id/linearLayout1">
        <Button android:layout_width="wrap_content"
            android:layout_height="wrap_content" android:layout_weight="0.5" android:textColor="#fff"
            android:id="@+id/btnLogin" android:text="Login" android:background="@drawable/my_button"></Button>
        <Button android:layout_width="wrap_content" android:background="@drawable/cancel_button"
            android:layout_height="wrap_content" android:layout_weight="0.5" android:textColor="#fff"
            android:id="@+id/btnCancel" android:text="Cancel"></Button>
    </LinearLayout>
</LinearLayout>
</RelativeLayout>